import React from 'react'
// import noteContext from '../context/noteContext';
function About() {
    // const hook = useContext(noteContext); 
    return (
        <div>
            this is about
            {/* I'am {hook.name} and iam from {hook.branch} branch */}
        </div>
    )
}

export default About
